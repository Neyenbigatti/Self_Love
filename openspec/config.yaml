# openspec/config.yaml
# Auto-generated by sdd-init — do not edit manually unless you know what you're doing.
schema: spec-driven

context: |
  Tech stack: Next.js 16.2 (App Router), TypeScript 5.7 (strict), Tailwind CSS v4, Drizzle ORM 0.45 + SQLite (libsql/Turso)
  Architecture: App Router pages & API routes, Drizzle schema-first DB, JWT auth (jose + bcryptjs), Radix UI + shadcn/ui patterns
  Testing: NONE — no test runner, no test files, no CI detected
  Style: shadcn/ui conventions (CVA, clsx, tailwind-merge, lucide-react), React Hook Form + Zod validation

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: "npm run build"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas

testing:
  strict_tdd: false
  reason: "No test runner detected in package.json scripts or devDependencies. Install a runner (vitest or jest) and add test scripts to enable Strict TDD."
  runner: null
  layers:
    unit:
      available: false
      tool: null
    integration:
      available: false
      tool: null
    e2e:
      available: false
      tool: null
  coverage:
    available: false
    command: null
  quality:
    linter:
      available: true
      command: "npm run lint"
    type_checker:
      available: true
      command: "npx tsc --noEmit"
    formatter:
      available: false
      command: null
